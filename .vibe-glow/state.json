{
  "schemaVersion": 1,
  "app": "STAR",
  "stage": "waves",
  "adapter": "web-playwright",
  "evidenceDir": "docs/ui-evidence",
  "designLanguagePath": "docs/superpowers/specs/2026-08-10-star-design-language.md",
  "findingsRegisterPath": "docs/superpowers/research/2026-08-10-star-ui-audit-findings.md",
  "themes": [
    "default"
  ],
  "invariants": [
    "Manila (#D2B98C) owns more than 40% of the room's pixel area in the filed state — a finding that shrinks cards to accent-sized chips is rejected however much cleaner it looks. The cards are the page, the cabinet is the frame.",
    "Aniline (#5C3D91) is a flat stamp-ink solid, never a gradient, never a brand wash — the gradient is the AI tell of 2026, not the hue. Any proposal introducing a violet gradient or accent wash is rejected on sight.",
    "Onionskin (#E9E2D2) stays a component inside an opened drawer, never a page ground — promoting it to a page background lands the direction in the cream-and-serif default by the side door.",
    "The stamp stays typographic: no distress textures, no rotation past 2.5 degrees — texture reads as a Photoshop filter, and decoration is what a skeptic dismisses.",
    "Contrast is measured, not eyeballed — every CSS file carries a recorded table against a 4.5:1 floor for text and 3:1 for controls and visual boundaries. A finding that moves a colour pair must restate the measured ratio or it is not shippable.",
    "Zero third-party requests at runtime — fonts and JS libraries are vendored under web/vendor/. No finding may introduce a CDN link or any hosted asset.",
    "No build step — web/ is hand-written CSS and ES modules served straight off a StaticFiles mount. No framework, no Tailwind, no bundler proposals.",
    "The prefers-reduced-motion override must stay AFTER the default in web/tokens.css — both rules target :root at equal specificity, so source order decides. Reordering silently defeats the accessibility feature; it already shipped broken once.",
    "Copy never says \"verified\", never promises a duration, never flatters the user — institutional register, evidence-first. DIRECTION.md's \"What the UI is now obliged to do\" outranks any copy finding."
  ],
  "waves": [
    {
      "id": 1,
      "name": "runs-and-work",
      "status": "clean",
      "findings": [
        "F-001",
        "F-003",
        "F-004",
        "F-005",
        "F-013"
      ],
      "rationale": "The five findings where a user loses work, loses a credential, or keeps paying for a run they can no longer see. One blast radius across app.js, shell.js, scriptcheck.js, account.js and auth.js, and four of the five are small edits against machinery the app already ships.",
      "executor": "vibe-cartographer",
      "branch": "glow/wave-1-runs-and-work",
      "cartCycle": 20,
      "closeOut": "ran 2026-08-11 pre-merge, in two passes. The first closed F-004, F-005 and F-013 and found a defect the wave introduced: setLiveRun(null) and clearStashedRun() were missing from the abandon path, so walking away from a run left the rail routing to an emptied progress panel for a run still spending. Fixed in resetProgress and guarded by a test that fails when either line is removed. A third build then closed F-001 and F-003, re-captured 06-progress-running, and verified the abandon path live. Seven comment-drift items fixed.",
      "closedClean": [
        "F-001",
        "F-003",
        "F-004",
        "F-005",
        "F-013"
      ]
    },
    {
      "id": 3,
      "name": "without-sight-or-a-mouse",
      "status": "open",
      "findings": [
        "F-012",
        "F-006",
        "F-010",
        "F-009",
        "F-021"
      ],
      "rationale": "The five findings that make the app usable without sight or a mouse. Chosen over the higher-ranked F-002 because that one still carries two unresolved design questions, and over the plugin's consistency-first default because those four render byte-identical pixels and make nothing downstream cheaper. This class is invisible in all 14 captures and undetectable by axe or Lighthouse, which is why it is still here.",
      "branch": "glow/wave-3-without-sight-or-a-mouse"
    }
  ]
}
